export default function Page() {
  return (
    <main style={{ padding: 40, fontFamily: 'monospace' }}>
      <h1>Augur Terminal — booting</h1>
      <p>API: /api/markets · /api/signals · /api/stream · POST /api/agent/[source]/[marketId]</p>
    </main>
  )
}
